CREATE TABLE `compra` (
    `compra_id` INT NOT NULL AUTO_INCREMENT,
    `compra_codigo` VARCHAR(100) NOT NULL,
    `compra_fecha` DATE NOT NULL,
    `compra_hora` TIME NOT NULL,
    `compra_total` DECIMAL(30,2) NOT NULL DEFAULT 0.00,
    `compra_pagado` DECIMAL(30,2) NOT NULL DEFAULT 0.00,
    `compra_cambio` DECIMAL(30,2) NOT NULL DEFAULT 0.00,
    `usuario_id` INT NOT NULL,
    `proveedor_id` INT NULL,
    PRIMARY KEY (`compra_id`),
    UNIQUE KEY `compra_codigo` (`compra_codigo`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;

CREATE TABLE `compra_detalle` (
    `compra_detalle_id` INT NOT NULL AUTO_INCREMENT,
    `compra_detalle_cantidad` INT NOT NULL,
    `compra_detalle_precio_compra` DECIMAL(30,2) NOT NULL,
    `compra_detalle_precio_venta` DECIMAL(30,2) NOT NULL DEFAULT 0.00,
    `compra_detalle_total` DECIMAL(30,2) NOT NULL,
    `compra_detalle_descripcion` VARCHAR(255) NOT NULL,
    `compra_codigo` VARCHAR(100) NOT NULL,
    `producto_id` INT NOT NULL,
    PRIMARY KEY (`compra_detalle_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;